# 仮説を残し、実績から見直すための改修記録

対象は2026年9月22日に確認した更新元 `9cfcabb`。元の作業フォルダには未保存の収集データがあるため、専用の作業場所で変更する。

## 完成させるもの

1. 社会・会社・個人の三つの目的、五つの未来、危険な経路、測る指標を平易な言葉で定義する。
2. 短期（1〜3か月）、中期（3か月超〜1年）、長期（1年超〜3年）をつなぐ。制度の変化は3〜10年も追う。
3. 仮説と期限付き予測を区別する。発行時の定義・期限・見込みを保存し、修正は別の記録にする。
4. 観測不足、未公表、不成立を区別して判定する。確率の誤差と原因を見直し、関連する長期の前提だけを更新する。
5. 収集・主分析・反証・統合判断は変更案を作る。正式記録への反映は検証プログラムだけが行う。失敗・再実行・並行実行で履歴を壊さない。
6. 日次処理から週次・月次・四半期の振り返りにつなぐ。現在の日次起動時刻を維持し、新しい通知先は作らない。
7. 9月15〜21日の日報7本と、企業5社・市場・シナリオのStatic Intelligence 7本を書き換える。旧版を保存し、過去日への予測の捏造や後付け採点をしない。
8. README、実行プロンプト、検査、日次ワークフロー、CLIを新設計に揃える。

## 必須の確認

| 確認すること | 合格を示す証拠 |
|---|---|
| 過去の予測を変えられない | 定義・期限・発行版の変更を拒否するテスト |
| 未来情報を予測に混ぜない | 公開・取得・情報締切・発行の時刻検査 |
| 観測不足を外れにしない | 公表待ち・欠測・条件不成立のテスト |
| 同じ処理を繰り返せる | 同一実行IDは記録を増やさず、内容違いと古い版の更新を拒否 |
| 誤差を再計算できる | 70%で不成立なら0.49。期間・版・同一事象群を分けて集計 |
| 訂正を追える | 旧実績・旧判定・初回と最新の採点が残る |
| 判定対象が同じ | 地域・母集団・単位・測定期間・品質の不一致を拒否 |
| 五つの未来は重なり得る | 合計100%への正規化や記事件数での増減を使わない |
| 壊れた変更案は公開しない | 検証失敗時に正式記録が変わらないテスト |
| 内部実績を公開しない | 公開対象の制限と、非公開指定の記録の拒否 |
| 精度改善を誇張しない | 判定件数・未判定率・比較対象・評価不足を表示 |
| 読みやすく根拠を追える | 全14本を読み直し、出典・留保・次の確認を照合 |

## 改修前に確認した問題

- 設定ファイルの長い説明に日次履歴が混ざっている。
- 収集失敗時に前日データをコピーし、統合失敗時に主分析を正式判断の代わりにしている。
- 検証失敗後も公開へ進む。モデルが正式設定を直接編集できる。
- 9月16日・21日の日報は生成失敗の通知だけ。書き換え時は取得できた資料を確認し、欠測を埋めたふりをしない。

## 完了した変更

- 小さな定義と追記専用台帳を分離した。21仮説を公開定義として移行し、支持・反証の観測を具体化した。既存の確度は予測の確率へ変換していない。
- 短期・中期・長期・探索の発行、実測、欠測、条件不成立、統計訂正、採点、原因の見直しを実装した。五つの未来は併存し、合計100%にしない。
- 日次処理を読み取り専用のモデル4段階と、決定的な検証・保存・表示に分けた。08:00 JSTと週・月・四半期・半年の分岐を維持した。
- 9月15〜21日の日報7本、Static 7本、週の振り返り1本、README、運用設計、6プロンプト、CLI、検査、GitHub Actionsを揃えた。
- 旧設定6本・旧日報7本・旧Static 7本を、元のハッシュとともに保存した。Evidence v1の生データは変更していない。
- 正式台帳に移行定義21件を登録した。期限付き予測の発行数は0件。基準不足の10候補を明示し、成績は未評価と表示する。

## 実施した検証（2026年9月22日）

36件のテストが通過した。外部APIを使わない一時ディレクトリで実行した。

| 確認 | 結果 |
|---|---|
| 定義・発行版の変更、古いrevision、同じIDの内容違い | 拒否し、台帳が変わらない |
| 公開・取得・発行時刻、期間・対象・品質の不一致 | 不正な案を拒否する |
| 未公表・欠測・条件不成立 | 当たり外れに混ぜない |
| 二択・数値の採点 | 70%の不成立は0.49。数値は単位別の誤差と区間内率 |
| 実績の訂正 | 初回0.49、訂正後0.09を別々に保持する例を確認 |
| 一連の運用 | 発行→実績→自動判定→成績表示→未レビューの判定を次の文脈へ戻す |
| モデル失敗・タイムアウト・反証の拒否・未反証の追加判断 | 正式判断の代用をせず、失敗を記録する |
| 表示段階の失敗 | 保存済みの案から復旧し、モデル再実行や二重保存をしない |
| 警戒の解除 | 同じ古い根拠だけでの引き下げを拒否する |
| 公開範囲 | private指定を拒否し、設定・認証・任意ファイルを公開対象にしない |
| 原資料の取得 | 公開ドメインのHTTPSのみ。実際の内容からハッシュを計算する |
| OpenCode設定 | 1.18.31のdebug configで解決済み設定を確認。編集・シェル・委任・外部フォルダはdeny |
| 文書 | 日報・Static全14本を原資料と照合。関連17文書のローカルリンク切れ0件 |
| 旧版の保存 | 20ファイルすべて元のSHA-256と一致 |
| 構造検査 | Python構文、JSON、JSON Schemaと移行記録、シェル構文、git diff --checkを確認 |

日報は各31〜37行、Staticは各35〜46行。根拠・未確認点・次の検証が近くに来るようにした。収集失敗の長い自己説明や、確信の強さへの固定パーセントは除いた。

## 残る運用上の条件

実装と検証は`codex/hypothesis-learning`の専用作業フォルダで行った。元の未保存データを持つ作業フォルダは変更していない。mainへ反映した後も、初期設定はshadowである。

過去のInformationを新しい日次処理が検索・自動参照するための変換と索引は未実装。旧原本を保存したことを、自動取り込みまで完了した意味には使わない。旧IDとの紐づけ、不明な日時の扱い、現在の判断への参照と過去の予測への情報混入の区別が追加対応として残る。

現在の環境にはGLM_API_KEYがないため、実モデルを使う一巡は未実施。OpenCodeの設定解決と模擬モデルでの一巡を確認した状態である。カバレッジ計測ライブラリもないため、カバレッジ率は未計測で、80%達成とは主張しない。

初期設定はshadow。14日分の成功を確認してからliveへ切り替える。これは運用の確認であり、予測精度の確認ではない。職種コード、基準値、自社・個人の実績、金額予算は未確定のまま残す。値を推定で埋めず、発行条件が揃った予測から評価を始める。
